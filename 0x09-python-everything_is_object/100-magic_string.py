#!/usr/bin/python3
"""
A function that returns a string n times the number of the iteration
"""


def magic_string(n=0):
    """
    Returns a string n times the number of the iteration
        """
    return ", ".join(["Holberton"] * n)
